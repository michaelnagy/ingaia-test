import styled from "styled-components";
import { flexRow, flexColumn, mobile, tablet } from "../../globalStyles";

export const SearchWrapper = styled.div`
  ${flexRow};
  justify-content: center;
`;

export const Input = styled.input``;
